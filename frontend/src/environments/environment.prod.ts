// Placeholder — no production backend has been deployed yet (see
// docs/04-ROADMAP.md, phase 16). Replace with the real API origin before
// building for production; a relative path assumes the frontend is served
// from the same origin as the API (e.g. behind a reverse proxy), which is
// the most common deployment shape but not guaranteed to be this project's.
export const environment = {
  production: true,
  apiBaseUrl: '/api/v1',
  wsBaseUrl: '',
};
